import { Button } from '..';
import { MenuIcon } from '../icons';

export const MobileNav = () => {
  return (
    <nav className="">
      <Button variant="link">
        <MenuIcon />
      </Button>
    </nav>
  );
};
